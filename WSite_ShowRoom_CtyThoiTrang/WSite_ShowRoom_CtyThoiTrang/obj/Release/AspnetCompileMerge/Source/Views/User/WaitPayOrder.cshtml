@model IEnumerable<WSite_ShowRoom_CtyThoiTrang.Models.tb_Order>




<div class="">
    <div class="row">
        <div class="col-xl-12">
            @if (Model != null && Model.Any())
            {
                foreach (var item in Model)
                {
                    var checkOutWareHouse = item.tb_KhoXuat.FirstOrDefault(x => x.OrderId == item.OrderId);


                    <p>@item.Code</p>

                    <div class="card border shadow-none IdOrder" id="@item.OrderId">
                        <div class="card-body">
                            @Html.Action("Partail_TrangThaiDonHang", "User", new { id = item.OrderId })

                            @Html.Action("Par_OrderDetailSuccess", "User", new { id = item.OrderId })
                            <div>
                                <div class="row" data-id="@item.OrderId">
                                    <div class="col-md-4">
                                        <div class="mt-3">
                                            @*<p class="text-muted mb-2">Price</p>
                                                <h5 class="mb-0 mt-2"><span class="text-muted me-2"><del class="font-size-16 fw-normal">$500</del></span>$450</h5>*@
                                        </div>
                                    </div>

                                    <div class="col-md-8">
                                        <div class="mt-8">

                                            <h5 class="text-muted mb-2">Tổng tiền : @WSite_ShowRoom_CtyThoiTrang.Common.Common.FormatNumber(item.TotalAmount)</h5>
                                        </div>
                                    </div>
                                    <div class="col-md-12">
                                        <div class="row">
                                            @if (item.Confirm == true)
                                            {

                                            }
                                            @if (item.typeOrder == false)
                                            {
                                                <div class="col-md-6">
                                                    <span style="color:lightslategray;width:5px" aria-label="Vui lòng chỉ nhấn " Đã nhận được hàng" khi đơn hàng đã được giao đến bạn và sản phẩm nhận được không có vấn đề nào." tabindex="0">
                                                        Vui lòng chỉ nhấn "Đã nhận được hàng" khi đơn hàng đã được giao đến bạn và sản phẩm nhận được không có vấn đề nào.
                                                    </span>
                                                </div>
                                                if (checkOutWareHouse != null)
                                                {
                                                    <div class="col-md-2">
                                                        <button type="button" class="btn btn-primary btnCancelOrder " disabled id="@item.OrderId">Hủy đơn hàng</button>
                                                    </div>

                                                    if (item.Success == true)
                                                    {
                                                        if (item.typeReturn == true)
                                                        {
                                                            <div class="col-md-2">
                                                                <button clas="btn btn-success">Đã Gửi Yêu Cầu </button>
                                                            </div>
                                                        }
                                                        else
                                                        {
                                                            <div class="col-md-2">
                                                                <a href="/User/ReturnOrder/@item.OrderId" clas="btn btn-danger" style="text-decoration:none;">Yêu Cầu Hoàn Trả </a>
                                                            </div>
                                                        }

                                                    }
                                                    else
                                                    {
                                                        <div class="col-md-2">
                                                            <button type="button" class="btn btn-primary btnSuccessOrder " data-id="@item.OrderId">Đã nhận đơn hàng</button>
                                                        </div>
                                                    }
                                                }
                                                else
                                                {
                                                    <div class="col-md-2">
                                                        <button type="button" class="btn btn-primary btnCancelOrder " id="@item.OrderId">Hủy đơn hàng</button>
                                                    </div>
                                                }



                                                <div class="col-md-2">
                                                    <button class="btn btn-dark">Liên Hệ Shop</button>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="col-md-6">
                                                    <span style="color:lightslategray;width:5px" aria-label="Đơn hàng đã hủy." tabindex="0">
                                                        Đơn hàng đã hủy
                                                    </span>
                                                </div>
                                            }
                                        </div>

                                    </div>



                                </div>
                            </div>

                        </div>
                    </div>



                }
            }
            else
            {
                <p> Không có bản ghi nào !!!</p>
            }

            <!-- end card -->

            <div class="row my-4">
                <div class="col-sm-6">
                    <a href="/home/index" class="btn btn-link text-muted">
                        <i class="mdi mdi-arrow-left me-1"></i> Tiếp tục mua hàng
                    </a>
                </div>
            </div> <!-- end row-->
        </div>


    </div>

</div>

